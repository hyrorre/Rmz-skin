local t = require("play")
t.header.type = 22
t.header.name = t.header.name .. " for4key"

local h, c = 			t.processHeader(t.header.type)
t.header.property = 	h.property
t.header.filepath = 	h.filepath
t.header.offset = 		h.offset
t.header.category = 	c

if skin_config then
	return t.main()
else
	return t.header
end
